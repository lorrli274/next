Exhausted iterators of [`array.array`](https://docs.python.org/3/library/array.html#array.array) will now stay exhausted even if the iterated array is extended. This is consistent with the behavior of other mutable sequences.

